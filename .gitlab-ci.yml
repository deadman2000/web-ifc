stages:
  - build

npm_publish:
  stage: build
  only:
    - aniklab
  image: emscripten/emsdk:2.0.25
  before_script:
    - cp .npmrc /root/.npmrc
  script:
    - npm i
    - npm run set-version
    - npm run build-release-all
    - npm set $NPM_TOKEN; cd dist; npm publish --access public
